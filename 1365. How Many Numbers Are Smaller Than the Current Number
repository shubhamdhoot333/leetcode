var smallerNumbersThanCurrent = function(nums) {
    var count=[];
    for(var i=0;i<nums.length;i++)
    {
        var value=0
        for(var j=0;j<nums.length;j++)
        {
            if(nums[i]>nums[j])
            {
                value++;
               
            }
        }
        count.push(value);
    }
   
    return count;
};
