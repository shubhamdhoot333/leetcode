var reverseWords = function(s) {
    var arr=[],arr1=[];
    arr=s.split(" ")
   
    for(var i=0;i<arr.length;i++)
    {
        arr1.push(arr[i].split("").reverse().join(""))
        
       
    }
    return arr1.join(" ")
};
