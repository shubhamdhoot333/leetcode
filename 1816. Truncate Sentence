var truncateSentence = function(s, k) {
    var arr=s.split(" "),arr1=[];
    
    for(var i=0;i<arr.length;i++)
    {
        if(i<k){
        arr1.push(arr[i]);
        }
    }
    return arr1.join(" ");
};
