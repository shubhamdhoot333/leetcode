var numOfStrings = function(patterns, word) {
    var count=0;
for(var i=0;i<patterns.length;i++)
{
    var value=word.includes(patterns[i]);
    if(value==true)
    {
        count++;
        
    }
    
}   
return count;
};
